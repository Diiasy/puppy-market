{{#each puppy}}
    <div>
        <a href="/puppies/detail/{{this._id}}">
            <img src="{{ this.mainPicturePath }}" alt="">
            <p>{{this.name}}</p>
        </a>
    </div>
{{/each}}

