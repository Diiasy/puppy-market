<div class="container my-2">
    <div class="row justify-content-center my-3">
        <div class="col-sm-8 col-md-6 col-lg-4">
            <h1>{{ puppy.name }}</h1>
        </div>
    </div>
    <div class="row justify-content-center my-3">
        <div class="col-sm-8 col-md-6 col-lg-4">
            <p>Breeds:</p>
            <ul>
                {{#each puppy.breed }}
                    <li>{{this}}</li>
                {{/each}}
            </ul>
            <p>Gender: {{ puppy.gender }}</p>
            <p>Birthday: {{ puppy.birthDate }}</p>
            <p>Colors:</p>
            <ul>
                {{#each puppy.colors }}
                    <li>{{ this }}</li>
                {{/each}}
            </ul>
            <p>Price: {{ puppy.price }}€</p>
            <p>Description: {{ puppy.description }}</p>
            <p>Owner: <a href="/users/profile?id={{puppy.owner._id}}" class="ref-link">{{ puppy.owner.username }}</a></p>
        </div>
        <div class="col-sm-8 col-md-6 col-lg-4">
            <img class="main-pic" src="{{ puppy.mainPicturePath }}" alt="">
            {{#if isOwner}}
                <button href="/puppies/update/{{puppy._id}}" class="btn btn-secondary m-1">Update puppy</button>
                <button href="/puppies/delete/{{puppy._id}}" class="btn btn-secondary m-1">Delete puppy</button>
            {{/if}}
        </div>
    </div>
    {{#if morePictures}}
        <div class="row justify-content-center my-3">
            <h4>Additional Pictures</h4>
        </div>

        <div class="row">
        {{#each puppy.picturesPath}}
            <div class="col-sm-8 col-md-6 col-lg-4 p-2 d-flex justify-content-center">
                <div class="card" style="width: 22rem">
                    <img src="{{ this }}" alt="" class="card-img-top" >
                </div>
            </div>
        {{/each}}
        </div>
    {{/if}}


    <div class="row justify-content-center my-3">
        <h4>Comments</h4>
    </div>

    <div class="row">
        <div class="col-sm-8 col-md-6 col-lg-6 p-2 d-flex justify-content-center"> 
            {{#each puppy.comments}}
                <p>{{this.content}}</p>
                <p>{{this.author.username}}</p>
            {{/each}}
        </div>
    </div>

    <div class="row d-flex justify-content-center my-3">
        <div class="col-sm-8 col-md-6">           
            <form action="/puppies/comment" method="POST">
                <div class="form-group">
                    <input type="text" class="form-control" id="content" name="content" placeholder="Your comment" >
                    <input type="text" hidden value="{{puppy.id}}" name="id">
                </div>

                <button type="submit" class="btn btn-secondary">Comment</button>

            </form>
        </div>
    </div>
</div>

